program : Nombre_Programa
data_type
 string: nombre;
 int: codigo;
 int: a = 500 , b = 200;
 float: dinero;
 string: s;
 string: dato;

begin
 read ( a , codigo );
 read ( a );
 if ( a != b != dato ) then
 end_if
 write ( "hola:" + nombre );
 read ( a );
 write ( codigo );
 write ( "Digita tu sueldo:" );
 read ( dinero );
 if ( dinero >= 1.0 & a | a ) then
 while ( nombre == nombre ) then
 end_if
 end_while
end
